basic algorithm:

use the score not present/absent of the wig file

sum of score in mutant/ sum of score in control >= 2 and 
sum of score in mutant >=  cutoff

or 
sum of score in control == 0 and sum of score in mutant >= zore_cutoff